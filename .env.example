# Supabase project configuration (backend only)
SUPABASE_URL=your-project-url
SUPABASE_SECRET_KEY=sb_secret_xxx
# Legacy fallback (optional during transition):
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# Optional runtime config
NODE_ENV=development
PORT=4000
FRONTEND_ORIGIN=http://localhost:3000
BACKEND_BASE_URL=http://localhost:4000

# Optional for SQL tools/migrations only (not required by backend runtime):
# SUPABASE_DB_URL=postgresql://postgres:password@db.your-project-ref.supabase.co:5432/postgres?sslmode=require
